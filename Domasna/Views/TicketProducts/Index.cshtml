@model List<Domasna.Domain.DomainModels.TicketProduct>

@{
    ViewData["Title"] = "Index";
}



<div class="container">

    @{
        for (int i = 0; i < Model.Count; i++)
        {
            var item = Model[i];

            if (i % 4 == 0)
            {
                @:<div class="row">
                }

                <div class="col-md-3 my-4">

                    <div class="card" style="width: 16rem; height: 20rem">

                        <img class="card-img-top img-fluid" src="@item.TicketProductImage" style="height: 50%" alt="Image for ticket!" />

                        <div class="card-body text-center">
                            <h3 class="card-title">@item.TicketProductName</h3>
                        </div>

                        <div class="m-3 text-center">
                            <a asp-controller="TicketProducts" asp-action="Details" asp-route-id="@item.Id" class="btn btn-dark m-1">View</a>
                        </div>

                    </div>
                </div>


                if (i % 4 == 3)
                {
                @:</div>
            }
        }
    }


</div>
